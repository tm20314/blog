---
import { url } from "@utils/url-utils";
import { profileConfig } from "@/config";
---

<footer class="editorial-footer">
	<p class="editorial-footer__statement">使って、作って、<br />ちゃんと残す。</p>
	<div class="editorial-footer__meta">
		<a class="editorial-footer__wordmark" href={url("/")}>つもログ</a>
		<nav class="editorial-footer__links" aria-label="フッターナビゲーション">
			<a href={url("/archive/")}>記事一覧</a>
			<a href={url("/about/")}>プロフィール</a>
			<a href={url("/rss.xml")}>RSS</a>
			{profileConfig.links.map((link) => (
				<a href={link.url} rel="me noopener" target="_blank">{link.name}</a>
			))}
		</nav>
		<p class="editorial-footer__legal">© {new Date().getFullYear()} TumoTumo · Built independently in Japan.</p>
	</div>
</footer>
